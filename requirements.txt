# Core dependencies (existing)
hidapi
mido
python-rtmidi
psutil

# REST API (Phase 3)
fastapi
uvicorn[standard]

# Windows UI automation & power control
pywinauto
pillow
pywin32

# MQTT/Home Assistant (Phase 5)
paho-mqtt
